#include <vector>
#include <math.h>
#include "symmetric_functions.h"

using namespace std;

void calculate_sf(const vector< vector<double> > &cartesians, const double& r_cutoff,
                const double& eta, const double& rs, const double& k,
                const int& lambda, const double& xi){
        for (int g_type = 1; g_type <= 5; g_type++){
            switch (g_type)
            {
            case 1:
                /* code */
                break;
            case 2:
                /* code */
                break;
            case 3:
                /* code */
                break;
            case 4:
                /* code */
                break;
            case 5:
                /* code */
                break;
            
            default:
                break;
            }
        }

}